// typed heading
const typed = new Typed('#typed',{
    stringsElement: '#typed-strings',
    backSpeed: 100,
    typeSpeed: 100,
    loop: true,
});

function fix() {
    if (condition) {
        
    } else {
        
    }
}
